Nothing much to say;
1. clone the 3 projects
2. go into each of them and build the Docker image (prefixed with `ml-`)
3. set up docker-compose yml
4. docker-compose up
5. go to URL and upload a random cucumber plushie photo I found off Google
